Every command should be tested according to this criteria:

    1. That they perform proper an valid input.
    2. That wildcard arguments are propery treated.
    3. That all arguments are used.
    4. That for all filename arguments
        * single paths
        * lists of paths
        * wildcards
        * list of wildcards
       are checked.
